import 'package:flutter/material.dart';

class NewBrandsView extends StatelessWidget {
  const NewBrandsView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
